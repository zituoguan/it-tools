```bash
#!/bin/bash
##############################################################################
# 快捷键与历史记录
##############################################################################

CTRL+A  # 移动到行首
CTRL+B  # 光标向后移动一个字符
CTRL+C  # 终止当前命令
CTRL+D  # 删除光标前一个字符或退出当前会话，类似 exit
CTRL+E  # 移动到行尾
CTRL+F  # 光标向前移动一个字符
CTRL+G  # 终止当前编辑命令并响铃
CTRL+H  # 删除光标下的字符（等同于 DELETE）
CTRL+J  # 等同于回车
CTRL+K  # 删除（剪切）从光标到行尾的内容
CTRL+L  # 清屏并重新显示当前行
CTRL+M  # 等同于回车
CTRL+N  # 历史命令中的下一条
CTRL+O  # 等同于回车，然后显示历史文件中的下一行
CTRL+P  # 历史命令中的上一条
CTRL+Q  # 恢复被挂起的 shell 输出
CTRL+R  # 向后搜索历史命令
CTRL+S  # 向前搜索历史命令或挂起 shell 输出
CTRL+T  # 交换两个字符
CTRL+U  # 删除（剪切）从光标到行首的内容
CTRL+V  # 使下一个输入的字符原样输入
CTRL+W  # 删除光标前的单词
CTRL+X  # 列出当前单词可能的文件名补全
CTRL+Y  # 粘贴（恢复）上次剪切的内容
CTRL+Z  # 挂起当前命令，使用 fg 恢复到前台或 bg 到后台

ALT+B   # 光标向后移动一个单词
ALT+D   # 删除下一个单词
ALT+F   # 光标向前移动一个单词
ALT+H   # 删除光标前一个字符
ALT+T   # 交换两个单词
ALT+.   # 粘贴上一条命令的最后一个单词，重复按可遍历历史
ALT+U   # 将光标到单词结尾的字符大写
ALT+L   # 将光标到单词结尾的字符小写
ALT+C   # 将光标下的字母大写，光标移到单词结尾
ALT+R   # 恢复已编辑的历史命令
ALT+?   # 列出可能的补全项
ALT+^   # 用历史中最近的匹配扩展当前行

CTRL+X 然后 (   # 开始录制键盘宏
CTRL+X 然后 )   # 结束录制键盘宏
CTRL+X 然后 E   # 调用上次录制的键盘宏
CTRL+X 然后 CTRL+E   # 用 $EDITOR 编辑当前命令行并执行结果
CTRL+A 然后 D  # 从 screen 注销但不关闭，命令继续运行

BACKSPACE  # 删除光标前一个字符
DELETE     # 删除光标下的字符

history   # 显示命令历史
!!        # 重复上一条命令
!<n>      # 引用第 n 条命令
!<string> # 引用以 string 开头的命令
esc :wq   # 退出并保存脚本

exit      # 退出当前会话


##############################################################################
# BASH 基础
##############################################################################

env                 # 显示所有环境变量

echo $SHELL         # 显示当前使用的 shell
echo $BASH_VERSION  # 显示 bash 版本

bash                # 切换到 bash（输入 exit 返回原 shell）
whereis bash        # 查找命令的二进制、源码和手册页
which bash          # 查找执行 'bash' 的实际路径

clear               # 清屏（隐藏已显示内容）


##############################################################################
# 文件操作命令
##############################################################################

ls                            # 列出当前目录下的文件，ls <dir> 指定目录
ls -l                         # 以详细格式列出文件（大小、所有者、权限、修改时间等）
ls -a                         # 列出所有文件（包括隐藏文件）
ln -s <文件名> <链接名>        # 创建符号链接
readlink <文件名>              # 显示符号链接指向的目标
tree                          # 以树状结构显示目录
mc                            # 终端文件管理器（类似 ncdu）
touch <文件名>                # 创建或更新文件
mktemp -t <文件名>            # 在 /tmp/ 创建临时文件（-d 创建目录）
cat <文件名>                  # 显示文件内容（原始输出）
cat -n <文件名>               # 显示带行号的内容
nl <file.sh>                  # 显示文件行号
cat 文件1 > 文件2             # 复制文件1到文件2
cat 文件1 >> 文件2            # 合并两个文件内容
any_command > <文件名>        # '>' 重定向命令输出到文件
more <文件名>                 # 分页显示文件内容（空格翻页，q 退出）
head <文件名>                 # 显示文件前几行（默认10行）
tail <文件名>                 # 显示文件最后几行（-f 实时跟踪）
vim <文件名>                  # 用 VIM 编辑文件，不存在则创建
mv <文件1> <目标>             # 移动或重命名文件
cp <文件1> <目标>             # 复制文件
rm <文件名>                   # 删除文件
find . -name <名称> <类型>     # 在当前及子目录查找文件或目录
diff <文件1> <文件2>          # 比较文件差异
wc <文件名>                   # 统计文件的行数、单词数和字符数（-lwc 只输出其中一项）
sort <文件名>                 # 按行排序文本文件（-n 数字排序，-r 逆序）
sort -t -k <文件名>           # 按指定分隔符和字段排序
rev                           # 字符串反转
chmod -选项 <文件名>          # 修改文件权限
gzip <文件名>                 # 用 gzip 压缩文件
gunzip <文件名>               # 解压 gzip 文件
gzcat <文件名>                # 查看压缩文件内容
lpr <文件名>                  # 打印文件
lpq                           # 查看打印队列
lprm <作业号>                 # 从打印队列移除作业
genscript                     # 文本转 postscript 并格式化打印
dvips <文件名>                # 打印 .dvi 文件（如 LaTeX 生成）
grep <模式> <文件>            # 查找文件中的字符串
grep -r <模式> <目录>         # 递归查找目录下的字符串
head -n file_name | tail +n   # 打印文件第 n 行
head -y lines.txt | tail +x   # 显示第 x 到 y 行

sed 's/<模式>/<替换>/g' <文件名> # 替换文件内容并输出到标准输出
sed -i 's/<模式>/<替换>/g' <文件名> # 原地替换文件内容
echo "this" | sed 's/is/at/g' # 替换输入流内容

##############################################################################
# 目录操作命令
##############################################################################

mkdir <目录名>                # 创建新目录
rmdir <目录名>                # 删除空目录
rmdir -rf <目录名>            # 删除非空目录
mv <目录1> <目录2>            # 重命名目录
cd                            # 切换到主目录
cd ..                         # 切换到上级目录
cd <目录名>                   # 切换目录
cp -r <目录1> <目录2>         # 递归复制目录
pwd                           # 显示当前路径
cd ~                          # 切换到主目录
cd -                          # 切换到上一个工作目录

##############################################################################
# SSH、系统信息与网络命令
##############################################################################

ssh user@host            # 以 user 连接到 host
ssh -p <端口> user@host  # 指定端口连接
ssh-copy-id user@host    # 添加 SSH 密钥，实现免密登录

whoami                   # 显示当前用户名
su <用户>                # 切换用户
su -                     # 切换到 root，通常需要 sudo su -
sudo <命令>              # 以 root 权限执行命令
passwd                   # 修改密码
quota -v                 # 显示磁盘配额
date                     # 显示当前日期和时间
cal                      # 显示当月日历
uptime                   # 显示系统运行时间
w                        # 显示在线用户
finger <用户>            # 显示用户信息
uname -a                 # 显示内核信息
man <命令>               # 查看命令手册
info <命令>              # 查看命令 info 文档
help                     # 查看内置命令帮助
df                       # 显示磁盘使用情况
du <文件名>              # 显示文件或目录磁盘占用（-s 仅显示总计）
resize2fs                # ext2/3/4 文件系统调整大小
last <用户名>            # 显示最近登录记录
ps -u <用户名>           # 显示用户进程
kill <PID>               # 杀死指定进程
killall <进程名>         # 杀死所有指定进程
top                      # 动态显示进程
lsof                     # 列出打开的文件
bg                       # 显示后台任务或将任务放到后台
fg                       # 将最近的后台任务调到前台
fg <作业>                # 指定作业调到前台

ping <主机>              # 测试主机连通性
whois <域名>             # 查询域名 whois 信息
dig <域名>               # 查询域名 DNS 信息
dig -x <主机>            # 反向查询主机
wget <文件>              # 下载文件
netstat                  # 显示网络连接、路由表等

time <命令>              # 统计命令执行时间


##############################################################################
# 变量
##############################################################################

变量名=值                     # 定义变量
变量名=值 命令                # 仅在该命令环境中定义变量
echo $变量名                  # 查看变量值
echo $$                       # 当前 shell 的进程号
echo $!                       # 最近后台进程的进程号
echo $?                       # 上一条命令的退出状态
read <变量名>                 # 从输入读取字符串赋值给变量
read -p "提示" <变量名>       # 带提示读取输入
column -t <文件名>            # 美观地按列显示内容（常与管道结合）
let <变量名>=<表达式>         # 数学运算（+、-、*、/、%）
export 变量名=值              # 定义环境变量（对子进程可见）
export -f <函数名>            # 导出函数
export var1="值"              # 导出并赋值
export <变量名>               # 导出变量
declare -x <变量名>           # 导出变量

array[0]=valA                 # 定义数组
array[1]=valB
array[2]=valC
array=([2]=valC [0]=valA [1]=valB)  # 另一种方式
array=(valA valB valC)              # 另一种方式

${array[i]}                   # 显示数组第 i 项
${#array[i]}                  # 数组元素长度
${#array[@]}                  # 数组元素个数

declare -a                    # 声明数组
declare -f                    # 仅显示函数名
declare -F                    # 显示函数名（不含定义）
declare -i                    # 声明整数变量
declare -r                    # 声明只读变量
declare -x                    # 标记为环境变量
declare -l                    # 变量值转小写
declare -A                    # 声明关联数组

${变量名:-word}               # 若变量存在且非空，返回其值，否则返回 word
${变量名:word}                # 同上
${变量名:=word}               # 若变量不存在则赋值为 word 并返回
${变量名:?message}            # 若变量不存在则输出 message 并终止
${变量名:+word}               # 若变量存在且非空，返回 word，否则返回空
${变量名:offset:length}       # 子串截取

${变量#模式}                  # 删除最短匹配前缀
${变量##模式}                 # 删除最长匹配前缀
${变量%模式}                  # 删除最短匹配后缀
${变量%%模式}                 # 删除最长匹配后缀
${变量/模式/字符串}           # 替换第一个匹配
${变量//模式/字符串}          # 替换所有匹配

${#变量名}                    # 变量值长度

*(模式列表)                   # 匹配零个或多个模式
+(模式列表)                   # 匹配一个或多个模式
?(模式列表)                   # 匹配零个或一个模式
@(模式列表)                   # 匹配一个模式
!(模式列表)                   # 匹配除指定模式外的内容

$(UNIX 命令)                  # 命令替换

typeset -l <x>                # 声明局部变量 - <x> 必须为整数

##############################################################################
# 函数
##############################################################################

# 通过位置参数引用传参，如 $1, $2 等。$@ 表示所有参数，$# 表示参数个数。

function 函数名() {
  shell 命令
}

unset -f 函数名  # 删除函数定义
declare -f       # 显示当前所有定义的函数

##############################################################################
# 流程控制
##############################################################################

语句1 && 语句2  # 与操作
语句1 || 语句2  # 或操作

-a              # test 条件表达式中的与
-o              # test 条件表达式中的或

# 字符串

str1 == str2               # 相等
str1 != str2               # 不等
str1 < str2                # 字典序小于
str1 > str2                # 字典序大于
str1 \> str2               # 排序后在 str2 之后
str1 \< str2               # 排序前于 str2
-n str1                    # 非空
-z str1                    # 为空

# 文件

-a 文件                    # 文件存在
-d 文件                    # 存在且为目录
-e 文件                    # 文件存在
-f 文件                    # 存在且为普通文件
-r 文件                    # 有读权限
-s 文件                    # 文件存在且非空
-w 文件                    # 有写权限
-x 文件                    # 有执行权限
-N 文件                    # 文件自上次读取后被修改
-O 文件                    # 文件属主为当前用户
-G 文件                    # 文件组与当前用户组相同
文件1 -nt 文件2            # 文件1 比文件2 新
文件1 -ot 文件2            # 文件1 比文件2 旧

# 数字

-lt                        # 小于
-le                        # 小于等于
-eq                        # 等于
-ge                        # 大于等于
-gt                        # 大于
-ne                        # 不等于

if 条件
then
  语句
[elif 条件
  then 语句...]
[else
  语句]
fi

for x in {1..10}
do
  语句
done

for name [in 列表]
do
  可用 $name 的语句
done

for (( 初始化 ; 结束条件 ; 更新 ))
do
  语句...
done

case 表达式 in
  模式1 )
    语句 ;;
  模式2 )
    语句 ;;
esac

select name [in 列表]
do
  可用 $name 的语句
done

while 条件; do
  语句
done

until 条件; do
  语句
done

##############################################################################
# 命令行处理流程
##############################################################################

# 命令查找顺序：函数 > 内建命令 > 脚本/可执行文件
# 可用 command、builtin、enable 覆盖默认顺序

command  # 跳过别名和函数，仅查找内建和 PATH 命令
builtin  # 只查找内建命令
enable   # 启用/禁用内建命令

eval     # 重新处理参数为命令行

##############################################################################
# 输入/输出重定向
##############################################################################

cmd1|cmd2  # 管道，将 cmd1 输出作为 cmd2 输入
< 文件     # 从文件读取输入
> 文件     # 输出重定向到文件
>> 文件    # 追加输出到文件
>|文件     # 强制输出到文件（即使设置了 noclobber）
n>|文件    # 指定文件描述符 n 强制输出
<> 文件    # 文件同时作为输入输出
n<>文件    # 文件描述符 n 同时输入输出
n>文件     # 文件描述符 n 输出到文件
n<文件     # 文件描述符 n 从文件输入
n>>文件    # 文件描述符 n 追加输出
n>&        # 标准输出复制到文件描述符 n
n<&        # 标准输入复制自文件描述符 n
n>&m       # n 复制 m 的输出
n<&m       # n 复制 m 的输入
&>文件     # 标准输出和错误输出都重定向到文件
<&-        # 关闭标准输入
>&-        # 关闭标准输出
n>&-       # 关闭文件描述符 n 的输出
n<&-       # 关闭文件描述符 n 的输入

|tee <文件># 同时输出到终端和文件（-a 追加）

##############################################################################
# 进程管理
##############################################################################

# 挂起任务：运行时按 CTRL+Z。CTRL+Y 也可挂起，但仅在进程尝试读取输入时生效。
# 中断任务：CTRL+C。

myCommand &  # 后台运行命令

jobs         # 列出所有作业（-l 显示 PID）

fg           # 将后台作业调到前台
fg %+        # 最近的后台作业
fg %-        # 倒数第二个后台作业
fg %N        # 指定编号作业
fg %string   # 命令以 string 开头的作业
fg %?string  # 命令包含 string 的作业

kill -l               # 显示所有信号
kill PID              # 终止指定进程
kill -s SIGKILL 4500  # 发送信号强制终止进程
kill -15 913          # 用信号15终止进程913
kill %1               # 按作业号终止

ps           # 显示当前 shell 及其进程信息
ps -a        # 显示所有带 tty 的进程

trap cmd sig1 sig2  # 脚本收到信号时执行命令
trap "" sig1 sig2   # 忽略信号
trap - sig1 sig2    # 恢复信号默认处理

disown <PID|JID>    # 从作业列表移除进程

wait                # 等待所有后台任务结束
sleep <秒数>        # 等待指定秒数

pv                  # 管道进度条
yes                 # 自动输入 yes

##############################################################################
# 技巧与小窍门
##############################################################################

# 设置别名
cd; nano .bash_profile
> alias gentlenode='ssh admin@gentlenode.com -p 3404'  # 添加别名

# 快速进入指定目录
cd; nano .bashrc
> shopt -s cdable_vars
> export websites="/Users/mac/Documents/websites"

source .bashrc
cd $websites

##############################################################################
# Shell 脚本调试
##############################################################################

bash -n 脚本名  # 只检查语法，不执行
set -o noexec   # 脚本内设置同上

bash -v 脚本名  # 执行前打印命令
set -o verbose  # 脚本内设置同上

bash -x 脚本名  # 执行时打印命令
set -o xtrace   # 脚本内设置同上

trap 'echo $变量名' EXIT  # 脚本退出时打印变量值

function errtrap {
  es=$?
  echo "ERROR 第 $1 行: 命令退出状态 $es."
}

trap 'errtrap $LINENO' ERR  # 命令出错时执行

function dbgtrap {
  echo "badvar is $badvar"
}

trap dbgtrap DEBUG  # 每条语句执行前触发
# ...问题代码段...
trap - DEBUG        # 关闭 DEBUG trap

function returntrap {
  echo "A return occurred"
}

trap returntrap RETURN  # 每次函数或脚本 return 时执行

##############################################################################
# 颜色与背景
##############################################################################
# 注：\e 或 \x1B 也可替代 \033
# 重置
Color_Off='\033[0m' # 重置文本颜色

# 常规颜色
Black='\033[0;30m'  # 黑色
Red='\033[0;31m'    # 红色
Green='\033[0;32m'  # 绿色
Yellow='\033[0;33m' # 黄色
Blue='\033[0;34m'   # 蓝色
Purple='\033[0;35m' # 紫色
Cyan='\033[0;36m'   # 青色
White='\033[0;97m'  # 白色

# 其他颜色
LGrey='\033[0;37m'  # 浅灰
DGrey='\033[0;90m'  # 深灰
LRed='\033[0;91m'   # 浅红
LGreen='\033[0;92m' # 浅绿
LYellow='\033[0;93m'# 浅黄
LBlue='\033[0;94m'  # 浅蓝
LPurple='\033[0;95m'# 浅紫
LCyan='\033[0;96m'  # 浅青

# 粗体
BBlack='\033[1;30m' # 黑色
BRed='\033[1;31m'   # 红色
BGreen='\033[1;32m' # 绿色
BYellow='\033[1;33m'# 黄色
BBlue='\033[1;34m'  # 蓝色
BPurple='\033[1;35m'# 紫色
BCyan='\033[1;36m'  # 青色
BWhite='\033[1;37m' # 白色

# 下划线
UBlack='\033[4;30m' # 黑色
URed='\033[4;31m'   # 红色
UGreen='\033[4;32m' # 绿色
UYellow='\033[4;33m'# 黄色
UBlue='\033[4;34m'  # 蓝色
UPurple='\033[4;35m'# 紫色
UCyan='\033[4;36m'  # 青色
UWhite='\033[4;37m' # 白色

# 背景色
On_Black='\033[40m' # 黑色
On_Red='\033[41m'   # 红色
On_Green='\033[42m' # 绿色
On_Yellow='\033[43m'# 黄色
On_Blue='\033[44m'  # 蓝色
On_Purple='\033[45m'# 紫色
On_Cyan='\033[46m'  # 青色
On_White='\033[47m' # 白色

# 使用示例
echo -e "${Green}这是绿色文本${Color_Off} 普通文本"
echo -e "${Red}${On_White}红色字白底${Color_Off}"
# -e 选项启用转义字符
printf "${Red} 这是红色 \n"
```
